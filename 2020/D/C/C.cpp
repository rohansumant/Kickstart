#include <bits/stdc++.h>
using namespace std;

void solve(int tc) {
  int n,a,b;
  cin >> n >> a >> b;
  vector<int> par(n+1);
  for(int i=2;i<=n;i++) {
    cin >> par[i];
  }
}
      
    

int main() {
  int tc; cin >> tc;
  for(int i=1;i<=tc;i++) solve(i);
}
